// Stub kept for shadcn CLI project detection.
// shadcn@4 inspects vite.config.* on disk to pick the framework preset
// (`npx shadcn add ...`). Renderer build config lives in electron.vite.config.ts.
export default {};
